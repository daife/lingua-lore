# Development

Install dependencies from the repository root:

```powershell
npm install
```

Run the desktop app:

```powershell
npm run dev
```

Build the Windows bundle:

```powershell
npm run build
```

This implementation was created without local build testing, per the project objective.
